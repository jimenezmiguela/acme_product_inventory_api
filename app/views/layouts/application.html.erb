<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ACME Product Inventory</title>

    <!-- CSRF and other meta tags -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Add Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= yield :head %> <!-- Yield any additional head content if defined in views -->
  </head>

  <body>
    <%= yield %> <!-- This is where the body content of each view is rendered -->
  </body>
</html>
